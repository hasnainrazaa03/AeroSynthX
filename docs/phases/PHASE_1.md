# Phase 1 — Architecture Spec + Deterministic Physics Core

Target release: `v0.1.0`.
Status: Planned.

This file will be expanded at the start of Phase 1. The high-level
outline lives here so dependencies on it are explicit.

## Deliverables (summary)

- `docs/ARCHITECTURE.md` finalized.
- `src/aerosynthx/physics/`:
  - ISA atmosphere.
  - Unit normalization (`pint`).
  - Reynolds, Mach, dynamic pressure.
  - Typed validation errors.
- ≥ 90% line coverage on `aerosynthx.physics`.
- Reference tests vs published ISA tables.

Detailed checklist will be added when Phase 0 closes.
